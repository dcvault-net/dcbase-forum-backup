# secrets / local state -- never commit
.wrangler/
.claude/
.dev.vars
.env
.env.*
*.env
*.secret
*_token*
*.pem

# node
node_modules/

# local rebuild working files (phpBB crawl output, copied assets, D1 seed).
# the site is rebuilt from the phpBB DB dumps, which are kept offline, not here.
recovery/

# OS / editor cruft
Thumbs.db
.DS_Store
desktop.ini
*.swp
*~
